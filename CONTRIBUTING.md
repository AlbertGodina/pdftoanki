# Contribuir al Projecte

Gràcies per considerar contribuir a aquest projecte! 🎉

## Com contribuir

### Reportar bugs

Si trobes un error:

1. Comprova que no existeixi ja un issue similar
2. Obre un nou issue amb:
   - Descripció clara del problema
   - Passos per reproduir-lo
   - Comportament esperat vs. comportament actual
   - Captures de pantalla si és possible
   - Informació del navegador i sistema operatiu

### Proposar millores

Per suggerir noves funcionalitats:

1. Obre un issue amb l'etiqueta "enhancement"
2. Explica clarament la funcionalitat proposada
3. Descriu per què seria útil
4. Si és possible, mostra exemples d'ús

### Fer canvis al codi

1. **Fork** el repositori
2. **Clona** el teu fork localment
3. Crea una **branca** per la teva feature:
   ```bash
   git checkout -b feature/nom-de-la-feature
   ```
4. Fes els teus canvis
5. **Testa** els canvis localment
6. **Commit** els canvis amb missatges descriptius:
   ```bash
   git commit -m "Afegeix: descripció breu del canvi"
   ```
7. **Push** a la teva branca:
   ```bash
   git push origin feature/nom-de-la-feature
   ```
8. Obre un **Pull Request**

## Guia d'estil

### Codi JavaScript

- Utilitza `const` i `let`, mai `var`
- Noms de variables en camelCase
- Funcions amb noms descriptius
- Comenta el codi complex
- Manté les funcions petites i enfocades

### CSS

- Utilitza variables CSS per colors i mides
- Manté la coherència amb els estils existents
- Comenta seccions complexes

### HTML

- Estructura semàntica
- Accessibilitat (ARIA labels on sigui necessari)
- Etiquetes tancades correctament

## Traduccions

Si vols ajudar amb les traduccions:

1. Tots els textos visibles haurien d'estar en català
2. Missatges d'error clars i comprensibles
3. Evita anglicismes innecessaris

## Proves locals

Per provar l'aplicació localment:

```bash
# Amb Python
python -m http.server 8000

# Amb Node.js
npx serve

# Obre http://localhost:8000
```

## Codi de conducta

- Sigues respectuós amb tots els col·laboradors
- Accepta crítiques constructives
- Centra't en el millor per al projecte
- Mostra empatia amb altres membres de la comunitat

## Preguntes?

Si tens qualsevol dubte, no dubtis a:

- Obrir un issue amb l'etiqueta "question"
- Contactar amb els mantenidors del projecte

Gràcies per contribuir! 💙
